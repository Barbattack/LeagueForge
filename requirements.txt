# =============================================================================
# LeagueForge - Requirements
# =============================================================================
# Installa con: pip install -r requirements.txt
# Aggiorna con: pip install --upgrade -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# WEB FRAMEWORK
# -----------------------------------------------------------------------------
Flask>=2.3.0,<3.0.0              # Web framework principale
Werkzeug>=2.3.0,<3.0.0           # WSGI utilities (incluso con Flask)
gunicorn>=21.0.0,<22.0.0         # Production WSGI server (per deploy)

# -----------------------------------------------------------------------------
# GOOGLE SHEETS API
# -----------------------------------------------------------------------------
gspread>=5.10.0,<6.0.0           # Client Google Sheets
google-auth>=2.22.0,<3.0.0       # Autenticazione Google Cloud

# -----------------------------------------------------------------------------
# DATA PROCESSING
# -----------------------------------------------------------------------------
pandas>=2.0.0,<3.0.0             # Manipolazione dati (import scripts)
rapidfuzz>=3.0.0,<4.0.0          # Fuzzy string matching per nomi giocatori

# -----------------------------------------------------------------------------
# TESTING (dev only)
# -----------------------------------------------------------------------------
pytest>=7.4.0,<8.0.0             # Test framework
pytest-cov>=4.1.0,<5.0.0         # Coverage reports

# -----------------------------------------------------------------------------
# NOTE VERSIONI
# -----------------------------------------------------------------------------
# - Flask 2.x: Stabile, supporto Python 3.8+
# - gspread 5.x: API v4, batch operations
# - pandas 2.x: Performance migliorate, richiede Python 3.9+
#
# Se usi Python 3.8, usa: pandas>=1.5.0,<2.0.0
# =============================================================================
